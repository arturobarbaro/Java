import java.util.Comparator;

public class Telefono implements Comparator{
    @Override
    public int compare(Object o1, Object o2) {
        return compare(o1,o2);
    }

    public int compareTo(Telefono telefono) {
        return this.compareTo(telefono);
    }
}
